---
stack_tool:
  name: terraform
  parameters:
    - cloud_organization

stack_instance_parameters:
  - website_name
  - instance_name
  - unique_id

platform:
  name: aws
  parameters:
    - aws_region
    - aws_profile
